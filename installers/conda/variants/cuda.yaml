# IOWarp Core - CUDA Variant
# CUDA-enabled debug build using CMakePresets.json "cuda-debug" preset
#
# Usage: IOWARP_PRESET=cuda-debug conda build installers/conda/ -c conda-forge

preset:
  - cuda-debug

cuda_enabled:
  - "ON"
rocm_enabled:
  - "OFF"
mpi_enabled:
  - "OFF"
mpi_impl:
  - nompi
