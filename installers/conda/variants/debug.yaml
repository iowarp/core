# IOWarp Core - Debug Variant
# Debug build using CMakePresets.json "debug" preset
#
# Usage: rattler-build build --recipe recipe/ --variant-config variants/debug.yaml

preset: debug

cuda_enabled: "OFF"
rocm_enabled: "OFF"
mpi_enabled: "OFF"
mpi_impl: nompi
